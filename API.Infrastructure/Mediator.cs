using System.Collections.Concurrent;

namespace API.Web.Infrastructure
{
    public class Mediator //: IMediator
    {
        //private static readonly ConcurrentDictionary<Type, object> _handlers = new ConcurrentDictionary<Type, object>();
        //public async Task<TResponse> Request<TResponse>(IQueryObject<TResponse> queryObject)
        //{
        //    if (queryObject == null)
        //    {
        //        throw new NotImplementedException();
        //    }
            
        //    var requestType = queryObject.GetType();

        //    var handler = 
        //}
    }
}
